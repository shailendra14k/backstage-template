- route:
    id: route-5711
    from:
      id: from-3753
      uri: "timer://foo"
      parameters:
        period: "1000"
      steps:
        - setBody:
            simple: 'Hello World'
        - log:
            id: log-9575
            message: "${body}"